version: '3.0'

services:
  ml:
    build: ml
    restart: always
    ports:
      - 8081:8000
  backend:
    build: backend
    restart: always
    ports:
      - 8082:8000
  nginx:
    image: nginx
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./Nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - web

networks:
  web:
    driver: bridge
